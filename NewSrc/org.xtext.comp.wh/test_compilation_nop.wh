
  
  function main :
  read DA, SA, BA
  % 
  	A:=nil;
  	B:=(cons nil nil nil nil SA DA);
  	C:=(cons nil nil nil (cons nil nil));
  	D:= A and B;
  	D1:= B and B;
  	E:= A or B;
  	E1:= A or A;
  	F:= not B;
  	F1:= not A;
  	G:= A =? B;
  	G1 := A =? A;
  	H:=(hd B);
	I:=(tl B);
	J:=(tl I);
	if A then 
		W:=B
	else
		W:=C
	fi;
	
  	 	
  	nop
  %
  write B, C, W
  
  function autre:
  read X, Y
  %
  	A:=B;
  	nop
  %
  write X, Y, A
  
